generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = "file:./dev.db"
}

model Session {
  id        Int      @id @default(autoincrement())
  createdAt DateTime @default(now())
  question  String
  answer    String
}
